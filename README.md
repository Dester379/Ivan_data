# Ivan_data

Adding all untracked file: 

  git init  
  git add .  
  git commit -m 'new files'  
  git push
  
To add one file:

  git init  
  git add 'filename'  
  git commit -m 'added smth'  
  git push  
